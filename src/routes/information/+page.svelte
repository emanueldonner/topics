<script>
	const getInfoFromUrl = (url) => {
		// get og info from url
	};

	const extractInfo = (url) => {
		const info = getInfoFromUrl(url);

		console.log('info', url);
	};
</script>

<p>hallo</p>

<!-- <input
	type="text"
	name="url"
	id="url"
	placeholder="paste url here"
	on:change={(e) => extractInfo(e.target.value)}
/> -->
